const contractModules = {
	state: {
		contractInfo: {}
	},
	mutations: {
		updateContract(state, contract) {
			state.contractInfo = contract
		}
	},
	actions: {

	}
}

export default contractModules
